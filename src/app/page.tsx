export default async function Home() {
  // const allUsers = await getUsers();

  return (
    <div className="h-screen flex items-center flex-col gap-5">
      {/* <UserButton />
      <div>
        <h1>All Users</h1>
        <ol>
          {allUsers?.map((_user: User) => (
            <li key={_user.id}>{_user.firstName}</li>
          ))}
        </ol>
      </div> */}
    </div>
  );
}
